# Changelog

## [1.0.8] - 2026-04-07

### Added
- **Modbus 6 位寻址全链路支持**: 实现了从前端 UI 到网关从站模拟器的完整 6 位寻址 (Modicon standard) 支持（如 `4xxxxx` 等 6 位大地址），解决了其导致的逻辑溢出、寻址截断及非法索引等核心顽疾。
- **从站模拟器交互大规模升级 (配合网关固件 1.3.1)**:
    - **双寻址基座对照**: 在模拟器中引入 `PLC 地址 (Base-1)` 与 `数据地址 (Base-0)` 表头明确区分，彻底消除通讯调试中常见的“偏移 1 位”困惑。
    - **实时十六进制换算**: 输入 PLC 地址时自动换算并显示协议偏移量的 16 进制形式（如 `410001` -> `2710H`），极大方便了工程师直接比对底层二进制报文。
    - **新手配置指南**: 网页端新增显著的配置提示，引导用户根据上位机（Master）寻址习惯灵活选用基准。
- **Modbus 数据可视化优化**:
    - **精准点位计数**: 修正了读取结果头部“点数统计”逻辑，过滤了结果总结行，并能智能根据功能码动态显示单位为“个寄存器”或“个点”。

### Changed
- **应用品牌标题**: 统一浏览器标题栏显示为“Anyport 调试工具”，提升了作为专业协议转换平台的品牌辨识度。
- **自适应布局重排**: 精细化控制了模拟器配置表的列宽（压缩冗余宽度、增加地址列显位），消除了各尺寸屏幕下的尴尬换行与内容错位。

## [1.0.7] - 2026-03-24

### Added
- **本地直连 Modbus TCP 模式**: 核心新特性。支持在桌面端通过 `Anyport Bridge` 直接调试局域网内的 Modbus TCP 设备（无需 ESP32 网关）。支持在 `Modbus Panel` 中一键切换 TCP 目标并保存配置。
- **通用通讯错误翻译引擎 (Translator)**: 引入了跨平台错误翻译工具，将底层的 Go, ESP32, 浏览器 API 生成的晦涩错误信息转换为对用户友好的中文诊断建议。
- **本地桥接程序 (Anyport Bridge) 健壮性增强**:
    - **会话隔离**: 实现了基于 WebSocket 会话的 TCP 连接池隔离，支持多标签页并发连接而不冲突。
    - **错误追异常功能**: 修复了读取协程异常时的静默失败问题，支持主动向前端推送 `ERROR:` 状态帧。
- **固件 1.2.2 特性深度适配**: 配合网关固件更新，实现了远程下发 W5500 目标地址（TCP Target IP/Port）、回传网关本机 IP 及链路状态的完整闭环。

### Fixed
- **连接意图识别系统**: 引入了 `explicitMqttConnected` 等识别标志，彻底解决了在切换协议或刷新页面时导致的 UI 连接状态误报。
- **诊断日志修复**: 修正了 Modbus 专用诊断视图中探测结果日志的存储路由，确保日志不随协议切换而丢失。

## [1.0.6] - 2026-03-24

### Added
- **Modbus 调试界面重构与模块化**:
    - 彻底重构了 `ModbusPanel.vue`，将其拆分为独立的 `Header`, `Control`, `Logs`, `Monitor` 高内聚组件，提升了代码的可读性与长期维护性。
    - **寄存器搜索下拉菜单**: 为 Modbus 寄存器选择引入了全新的搜索下拉框，支持实时名称过滤，并巧妙地将搜索框集成在下拉面板顶部，极大优化了多点位设备的调试效率。
- **UI 布局与视觉系统优化**:
    - **精确对齐**: 重新设计了 Base 0 / Base 1 切换按钮的布局，现已实现与 PLC 地址显示框的完美左对齐。
    - **紧凑模式 (Compact Mode)**: 压缩了整页边距、组件间距及标题字号，移除了最大宽度限制，使 Modbus 调试界面在视觉与操作体验上与 BACnet 界面保持高度一致。
    - **字节序显示优化**: 适度增加了数据类型旁的 Endian 切换框宽度，确保“CDAB”等 4 字节序标识不再截断。

### Fixed
- **写入确认逻辑重构 (Race Condition Fix)**: 移除了 `ModbusControl` 中不稳定的 `setTimeout` 延迟更新，改为基于 Rx 数据流的 `watch` 机制，确保写入确认弹窗中的“当前设备值”更新 100% 准确，彻底解决了由于网络延迟导致的旧值脏读问题。
- **版本号同步**: 统一了 Monorepo 下各子包的版本管理，确保分发一致性。

## [1.0.5] - 2026-03-12

### Added
- **BACnet 协议全流程支持**:
    - 新增 BACnet/MSTP 和 BACnet/IP 协议适配器，支持完整的协议栈封装与解析。
    - 实现 BACnet 服务层 (`BacnetService`)，支持网络扫描、属性读取及对象列表自动提取。
    - **专用调试面板**: 新增 `BacnetPanel.vue`，支持实时日志解析（自动处理分段握手）、CRC 校验及设备属性树状展示。
- **Modbus 位聚合 (Bits Aggregation)**:
    - 支持在点表中使用 `bits` 数据类型，将 16 位寄存器拆解为独立状态位，并支持自定义 mapping 映射。
    - **UI 展示增强**: 聚合位字段在数据视图中支持分行平衡显示（每行 2 个），极大程度优化了含大量位字段设备的 UI 布局。
- **本地桥接工具 (Anyport Bridge)**:
    - 新增 Go 语言开发的本地网桥工具，用于在隔离网络环境下建立调试隧道。
    - **自动化发布**: GitHub Actions 新增 Go 编译流程，支持在发布版本时自动打包 `anyport-bridge.exe` 为 Release 附件。

### Changed
- **Modbus 地址逻辑重构**: 彻底重构多协议下的地址归一化算法。移除了底层硬编码的“自动减一”，将地址基准完全交由 UI 层的 **Base 0/1** 按钮控制，完美兼容不同厂商的地址偏好，解决了 4xxxx 地址报错的问题。
- **Remote MQTT 体验优化**: 将默认连接模式由 TCP 调整为更常用的 **RTU**，并优化了协议切换按钮的 UI 顺序。
- **点表库管理增强**: 优化了描述信息的搜索与读取优先级，支持显示“已验证”等点表质量元数据。

### Fixed
- **ESP32 网关稳定性重构**:
    - 修复了 GPIO 引脚冲突（解决 USART1 与 CH343/W5500 SPI 引脚的竞争问题）。
    - 升级 MQTT 通讯为 WSS 协议，修复了连接证书校验失败的问题，并优化了 NTP 时间同步流程。
    - 增加 RS485 读前全清物理缓冲区功能，有效降低高频高负载下的通信报文重叠。
    - 支持以太网静态 IP 配置，增强了工业现场的部署灵活性。
- **构建体积优化**: 更新 `.dockerignore` 排除非运行时源码，生产环境 Docker 镜像体积显著减小。
- **依赖清理**: 统一同步主仓库与子包版本号，将冗余的 `.exe` 追踪记录从 Git 仓库中永久移除。

## [1.0.4] - 2026-02-19

### Added
- **架构升级 - 全面转向 MQTT**: 弃用了原有的不稳定 WebSocket 远程连接方案，完整实现了基于 MQTT 协议的跨地域远程调试传输层 (`MqttTransport`)。
- **网关自动发现与健康管理**:
    - 新增网关自动发现机制：通过监听 MQTT 通配符主题动态识别全局活跃网关。
    - 引入心跳监测系统：实时追踪网关负载与在线状态，超时（30秒）无响应自动标记离线。
- **SaaS 级网关管理器 (Gateway Manager)**:
    - **视觉升级**: 采用现代化 Table 布局重构对话框，支持查看 MAC、固件版本、IP 等多维信息。
    - **状态指示**: 为在线网关添加呼吸灯动效，增强实时感知。
    - **一键直连**: 支持在网关列表中直接点击图标回填配置并建立连接。
- **点表选择器 (Modbus Profile Picker)**: 新增独立的点表选择弹窗，支持从本地点表库快速搜索、预览并应用设备点表。
- **实时状态栏优化**: 重构顶部导航栏，在显眼位置展示 MQTT Broker 与网关的独立双状态连接指示。

### Changed
- **移除独立 WiFi 调试模式**: 正式取消原有的 WiFi Socket 直连模式。所有远程调试统一通过 MQTT 网关接入，大幅简化网络配置并提升内网穿透能力。
- **网关智能筛选**: 网关下拉列表现在支持根据输入的 Site ID 实时模糊过滤，优化多设备资产管理效率。
- **默认配置安全增强**: 将默认的 Site ID 和 Gateway ID 修改为空，防止新用户初始化时因产生干扰数据连接至公共测试设备。
- **图标体系迁移**: 全面集成 `lucide-vue-next` 图标库，替换了原有简陋的字符图标，显著提升视觉一致性。

### Fixed
- **幽灵网关修复**: 解决了由于 MQTT Retained 消息残留导致的页面刷新后出现停用网关记录的问题。
- **UI 布局缺陷**: 修复了浅色模式下操作按钮对比度低、图标被默认 Padding 挤压导致不可见等视觉 Bug。
- **组件完整性**: 补充了缺失的 `ModbusProfilePicker` 和 `GatewayManagerDialog` 组件，彻底解决构建阶段的类型引用错误。
- **地址偏移逻辑**: 优化了 Modbus 面板中 Base 0 / Base 1 地址基准切换时的自动校验提示。

## [1.0.3] - 2026-02-11

### Added
- 完整实现 Modbus 调试主流程，包括 Modbus RTU、本地串口和调试面板。
- 新增 Modbus TCP 适配器与 WebSocket 网关传输层，支持通过网关远程调试。
- 新增 ESP32-C3 网关固件工程（`esp32/AnyPortGateway`），用于现场部署 AnyPort Gateway。

### Changed
- 更新 README 文档，完善 Modbus 调试功能说明与架构描述。
- 在桌面端侧边栏中显示当前版本号，便于快速确认部署版本。

### Fixed
- 优化 Modbus 调试数据解析逻辑，确保结果视图与通信日志显示一致。

## [1.0.2] - 2026-02-08

### Added
- **移动端 UI 体验优化**:
    - 重构移动端布局，引入侧边抽屉导航（Drawer）替代底部 Tab 栏。
    - 新增顶部 AppBar，包含实时连接状态指示与页面标题。
    - 为“点表库”页面添加完整的响应式布局适配，提升手机端编辑体验。
    - 引入页面切换平滑动画（Page Transitions）。

### Fixed
- **Modbus 悬浮窗修复**: 修复了报文结构解析和通信日志悬浮窗在部分布局下被截断的问题，并优化了动画流畅度，确保在 Windows 桌面端也能完美显示。

## [1.0.1] - 2026-02-08

### Added
- **Docker 支持**: 添加 Dockerfile，支持基于 Nginx 的容器化部署。
- **CI/CD**: 添加 GitHub Actions 工作流，实现 Tag 推送时自动构建并发布镜像到 GHCR。

### Fixed
- **类型定义**: 修复 `WebSerialTransport.ts` 和 `device.types.ts` 中的 TypeScript 错误，确保项目顺利构建。

## [1.0.0] - 2026-02-05

### Added
- **多协议架构**: 初始化项目，支持 Modbus RTU 协议，预留 DL/T645、MQTT 扩展接口。
- **本地轮廓管理 (Profile Manager)**:
    - 实现设备配置（轮廓）的本地持久化存储（基于 IndexedDB）。
    - 新增“轮廓库”页面，支持配置的保存、编辑、导入/导出。
    - 轮廓包含：通讯参数、点表定义、寄存器映射等。
- **Modbus 功能增强**:
    - **日志解析**: RX 报文 hover 自动解析功能，实时显示 Slave ID、功能码、数据内容及 CRC 校验结果。
    - **操作反馈**: 集成全局 Toast 通知，实时反馈读写指令的执行状态。
    - **交互优化**: 优化了 Modbus 面板的响应式布局，调整了控制按钮的视觉优先级。
- **Web Serial API**: 集成浏览器原生串口通信能力，无需后端代理。
- **PWA 支持**: 配置 Vite PWA 插件，支持应用离线安装与使用。
- **工程化配置**:
    - 采用 Monorepo 结构 (`frontend` + `shared`)。
    - 配置 TypeScript, Vite, Pinia。
    - 添加 GitHub 标准化文件 (`README`, `LICENSE`, `.gitignore`)。

### Changed
- **UI/UX 改进**:
    - 移动端侧边栏头部视觉优化，增强层级感。
    - 登录/配置页面的布局调整，适配更多屏幕尺寸。
    - 响应式设计：实现桌面端与移动端的独立布局与交互优化。
- **README 更新**: 补充了详细的项目技术架构说明。

### Fixed
- **Vite 构建**: 修复 Monorepo 结构下 `shared` 包 TypeScript 文件无法被前端项目直接引用的问题。
- **安全上下文**: 增加对非安全上下文 (HTTP IP) 的检测，并提供明确的错误提示引导用户使用 Localhost 或 HTTPS。
